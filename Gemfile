source "https://rubygems.org"

# Use GitHub Pages' curated Jekyll stack for local dev parity
gem "github-pages", group: :jekyll_plugins

# Needed on some Ruby versions when running `jekyll serve`
gem "webrick", "~> 1.8"
